name: Daily Certification Health Check

on:
  schedule:
    - cron: '0 13 * * *' # 6:00 PM PKT (UTC+5) daily
  workflow_dispatch: {}

jobs:
  test-and-email:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm ci
          npx playwright install --with-deps

      - name: Prepare report directory
        run: |
          mkdir -p report

      - name: Run DEMO Environment Tests
        env:
          DEMO_URL: ${{ vars.DEMO_URL || 'https://demo.certified.io' }}
        run: |
          npx playwright test --project=demo
          cp report/results.json report/demo-results.json

      - name: Run ETRAINING Environment Tests
        env:
          ETRAINING_URL: ${{ vars.ETRAINING_URL || 'https://etraining45512.certified.io' }}
        run: |
          npx playwright test --project=etraining
          cp report/results.json report/etraining-results.json

      - name: Run ETRAINING-STAGE Environment Tests
        env:
          ETRAINING_STAGE_URL: ${{ vars.ETRAINING_STAGE_URL || 'https://etraining-stage.certified.io' }}
        run: |
          npx playwright test --project=etraining-stage
          cp report/results.json report/etraining-stage-results.json

      - name: Run AIA Environment Tests
        env:
          AIA_URL: ${{ vars.AIA_URL || 'https://aia45775.certified.io' }}
        run: |
          npx playwright test --project=aia
          cp report/results.json report/aia-results.json

      - name: Run AIA-STAGE Environment Tests
        env:
          AIA_STAGE_URL: ${{ vars.AIA_STAGE_URL || 'https://aia-stage.certified.io' }}
        run: |
          npx playwright test --project=aia-stage
          cp report/results.json report/aia-stage-results.json

      - name: Run AIFT Environment Tests
        env:
          AIFT_URL: ${{ vars.AIFT_URL || 'https://aift45665.certified.io' }}
        run: |
          npx playwright test --project=aift
          cp report/results.json report/aift-results.json

      - name: Run AIFT-STAGE Environment Tests
        env:
          AIFT_STAGE_URL: ${{ vars.AIFT_STAGE_URL || 'https://aift-stage.certified.io' }}
        run: |
          npx playwright test --project=aift-stage
          cp report/results.json report/aift-stage-results.json

      - name: Generate Combined Report
        run: |
          node scripts/generate-combined-report.js

      - name: Upload HTML report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: certification-health-report
          path: report/

      - name: Send Health Check Email
        if: always()
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
        run: |
          node scripts/send-health-check-report.js